pandas>=1.3
numpy>=1.21
scikit-learn>=1.0
matplotlib>=3.4
mplfinance>=0.12.7a0
Pillow>=8.0
torch>=1.9.0
torchvision>=0.10.0

# Note: Installing `torch`/`torchvision` is platform and CUDA dependent.
# For CPU-only Windows, consider: `pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu`